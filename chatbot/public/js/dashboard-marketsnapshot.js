import { renderStockHero } from "./dashboard-techindicators.js";

// Currently composes shared components. 
// Provides a dedicated module for Market Snapshot specific logic/overrides in the future.

export { renderStockHero };
